apiVersion: v1
kind: Service
metadata:
  name: iam-identitymanagement-service-public
spec:
  type: ClusterIP
  selector:
    app: iam-identitymanagement-service
  ports:
  - name: http
    port: 2080
    targetPort: 2080
  - name: https
    port: 443
    targetPort: 443
